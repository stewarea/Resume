<html>
<head>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
    <title>My Jobs</title>
</head>
<body>
    <div class="container">
        <h1>Where and What I've Done</h1>

        {% if jobs is not empty %}
            <ul class='list-group'>
                <li class='list-group-item active'>My Trail of Disgrace</li>
                {% for job in jobs %}
                    <li class='list-group-item'>{{ job.getJobSummary }}</li>
                {% endfor %}
            </ul>
        {% endif %}

        <form action='/jobs' method='post'>
            <div class="form-group">
                <label for='jobTitle'>Job Title</label>
                <input id='jobTitle' name='jobTitle' type='text' class='form-control'>
            </div>
            <div class="form-group">
                <label for='jobEmployer'>Employer</label>
                <input id='jobEmployer' name='jobEmployer' type='text' class='form-control'>
            </div>
            <div class="form-group">
                <label for='jobStart'>When Did You Start This Gig?</label>
                <input id='jobStart' name='jobStart' type='number' class='form-control'>
            </div>
            <div class="form-group">
                <label for='jobEnd'>When Did You End This Gig?</label>
                <input id='jobEnd' name='jobEnd' type='number' class='form-control'>
            </div>
            <button type='submit' class='btn btn-info'>Add Job</button>
        </form>
        <form action='/clear_jobs' method='post'>
            <button type='submit' class='btn btn-warning'>Forget Everything</button>
        </form>
    </div>
</body>
</html>
